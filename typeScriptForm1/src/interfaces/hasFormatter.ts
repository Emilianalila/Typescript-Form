export interface HasFormatter { // implements is used when you want to make sure that a class has certain properties and methods defined by an interface,
  format():string; // this function has to return a string
}